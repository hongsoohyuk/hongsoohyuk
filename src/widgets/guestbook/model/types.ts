export type EntriesText = {
  headerTitle: string;
  headerSubtitle: string;
  empty: string;
  fetchError: string;
  pagination: {
    previous: string;
    next: string;
    summary: string;
  };
  retry: string;
};
